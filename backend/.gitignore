node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Uploaded product images (dev-only local storage; see note in products.ts)
/uploads/*
!/uploads/.gitkeep
